using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CaselleController : MonoBehaviour {


	public float timer;

/*
	private Animator anim;
	int num; 					//numero casella
	//bool exit;



	void Start () {
		anim = this.GetComponent<Animator>();
		num = int.Parse(this.GetComponentInChildren<Text>().text);
	}
	

	void Update () {
		
	}


	void OnTriggerEnter2D(Collider2D collider){

		if ( (collider.gameObject.tag == "InGame") && (anim.GetBool("ZoomOut") == false) ){
			anim.SetBool("ZoomOut", true);
			DropController.casellaVuota = num;
			GameController.ScelteRandom(num);
		}
	}
		
*/

}
